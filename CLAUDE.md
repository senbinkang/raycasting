# 工作流规则（Boris Tane 工作流）

核心原则：**在审查并批准书面计划之前，绝不要写代码。** 先计划，后实现。

## 执行流程

任何非平凡的开发任务（新功能、重构、跨文件改动），严格按以下流水线执行：

1. **研究** — 深度阅读相关代码，写入 `doc/_active/YYYY-MM-DD-N-<主题>-研究.md`，不要只在聊天中总结
2. **规划** — 写详细实现计划到 `doc/_active/YYYY-MM-DD-N-<主题>-方案.md`，包含代码片段。先不要实现
3. **批注循环** — 我会在方案文档中添加内联批注，发回给你修改。重复 1-6 轮直到计划严谨
4. **任务拆分** — 在计划文档中添加详细的任务清单。先不要实现
5. **实现** — 计划批准后，完整实现。每完成一个任务在计划中标记完成。不要暂停。不要用 any/unknown。持续运行类型检查

## 实现约束

- **任何改动都先说明方案，等我审阅批准后再动手。绝不要直接改文件。先讲清楚改什么、怎么改、为什么，必要时问我要不要生成 md 文件。**
- 没有方案文档就不能写代码
- 所有 md 文件名必须写明主题，禁止用 `research.md` / `plan.md` 之类的泛名；遵循 `YYYY-MM-DD-<主题>-研究/方案.md` 格式
- 我处理判断决策，你处理机械执行
- 纠正只需一句话；走错方向就回滚缩小范围，不要老路上修补
- 前端问题优先贴截图 + 短反馈
- 指向已有实现比从零描述更精确
- 实现阶段保持整洁：不要多余注释，不要 jsdoc，严格类型

## 开始时

先读 `doc/_active/0-项目总览.md` 和 `doc/_active/1-未来方向.md` 了解项目进度，避免重复或冲突。

## 功能完成后

每个功能完成后，必须：
1. 更新 `doc/_active/0-项目总览.md`，将已完成项的状态从 ⏳ 改为 ✅，同步更新完成度百分比和文件索引
2. 检查 `doc/_archive/` 下相关执行方案文件，若文件名或内容有"未完成"等与实际情况不符的表述，及时修正
3. 生成操作记录到 `doc/_archive/YYYY-MM-DD-N-<主题>.md`
4. 生成 commit message 前，必须先执行 `git status` 和 `git diff`（含暂存区）查看所有待提交改动，确保 commit message 覆盖全部修改而非仅当前批次
5. 将 commit message 贴给我确认
6. 我批准后执行 `git commit`

## Commit Message 规范

```
type(模块): 中文简短描述（不超过 50 字）
- 关键改动点 1（文件：xxx.js）
- 关键改动点 2（文件：xxx.js）
```

type 取值：
- `feat` — 新功能
- `fix` — bug 修复
- `refactor` — 重构/性能优化
- `docs` — 文档
- `chore` — 工具/配置

## 操作记录模板

```markdown
# YYYY-MM-DD — <主题，不超过 10 字>

## 一、需求/问题
<2-5 行概括>

## 二、做了什么
- <关键动作 1>，涉及文件：`xxx.js`
- <关键动作 2>，涉及文件：`xxx.js`

## 三、关键决策 / 踩过的坑
<为什么选这个方案？有什么要注意？没有就写"无">

## 四、文件清单
| 操作 | 文件 | 说明 |
|------|------|------|
| 修改/新增/删除 | `相对路径` | <一句话> |
```
