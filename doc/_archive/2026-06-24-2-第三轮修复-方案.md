# 2026-06-24 — 第三轮修复 执行方案

## 1. Player.js

### 1.1 tryMove：7点采样 + 对角点

替换整个 `tryMove` 方法：

```javascript
tryMove(dx, dy, step) {
    let len = Math.sqrt(dx * dx + dy * dy)
    if (len < 0.0001) return
    dx = dx / len
    dy = dy / len

    let r = this.collisionRadius

    let newX = this.position.x + dx * step
    let newY = this.position.y + dy * step
    let checkX = newX + Math.sign(dx) * r
    let checkY = newY + Math.sign(dy) * r

    let canMoveX = true
    for (let offset of [0, 0.3, -0.3, 0.55, -0.55, 0.85, -0.85]) {
        let sy = this.position.y + offset * r
        if (!this.cellIsEmpty(checkX, sy)) { canMoveX = false; break }
    }
    if (canMoveX && dy !== 0) {
        let sy = this.position.y + Math.sign(dy) * r
        if (!this.cellIsEmpty(checkX, sy)) canMoveX = false
    }

    let canMoveY = true
    for (let offset of [0, 0.3, -0.3, 0.55, -0.55, 0.85, -0.85]) {
        let sx = this.position.x + offset * r
        if (!this.cellIsEmpty(sx, checkY)) { canMoveY = false; break }
    }
    if (canMoveY && dx !== 0) {
        let sx = this.position.x + Math.sign(dx) * r
        if (!this.cellIsEmpty(sx, checkY)) canMoveY = false
    }

    if (canMoveX) this.position.x = newX
    if (canMoveY) this.position.y = newY
}
```

### 1.2 cellIsEmpty：删除门检测

替换整个方法：

```javascript
cellIsEmpty(x, y) {
    let mx = Math.floor(x)
    let my = Math.floor(y)
    if (mx < 0 || my < 0 || mx >= this.columns || my >= this.lines) return false
    return this.worldMap[my][mx] === 0
}
```

### 1.3 删除 E 键 + tryInteractDoor

- 删除 `g.registerAction('e', (dt) => this.tryInteractDoor())` 这一行
- 删除整个 `tryInteractDoor()` 方法

### 1.4 drawDirArrow：删除 FOV 边界线

删除 leftX/rightX 两段 drawLine 代码，只保留黄色朝向箭头。

### 1.5 drawRays：步长 10° → 5°

```javascript
let totalSteps = Math.round(2 * halfFov / (5 * Math.PI / 180))
```

---

## 2. game/Sprite.js

### 2.1 敌人移动：5点采样 + 对角点 + 去门

替换 `update()` 中移动部分（第 85-108 行）：

```javascript
let r = this.radius || 0.2
let moveX = this.x + ndx * this.speed * dt
let moveY = this.y + ndy * this.speed * dt

let checkX = moveX + Math.sign(ndx) * r
let canMoveX = true
for (let offset of [0, 0.4, -0.4, 0.75, -0.75]) {
    let sy = this.y + offset * r
    let cx = Math.floor(checkX), cy = Math.floor(sy)
    if (cx >= 0 && cy >= 0 && cx < bg.columns && cy < bg.lines) {
        if (bg.worldMap[cy][cx] !== 0) { canMoveX = false; break }
    }
}
if (canMoveX && ndy !== 0) {
    let sy = this.y + Math.sign(ndy) * r
    let cx = Math.floor(checkX), cy = Math.floor(sy)
    if (cx >= 0 && cy >= 0 && cx < bg.columns && cy < bg.lines) {
        if (bg.worldMap[cy][cx] !== 0) canMoveX = false
    }
}
if (canMoveX) this.x = moveX

let checkY = moveY + Math.sign(ndy) * r
let canMoveY = true
for (let offset of [0, 0.4, -0.4, 0.75, -0.75]) {
    let sx = this.x + offset * r
    let cx = Math.floor(sx), cy = Math.floor(checkY)
    if (cx >= 0 && cy >= 0 && cx < bg.columns && cy < bg.lines) {
        if (bg.worldMap[cy][cx] !== 0) { canMoveY = false; break }
    }
}
if (canMoveY && ndx !== 0) {
    let sx = this.x + Math.sign(ndx) * r
    let cx = Math.floor(sx), cy = Math.floor(checkY)
    if (cx >= 0 && cy >= 0 && cx < bg.columns && cy < bg.lines) {
        if (bg.worldMap[cy][cx] !== 0) canMoveY = false
    }
}
if (canMoveY) this.y = moveY
```

### 2.2 接触伤害用 this.damage 字段

替换现有的 `player.takeDamage(20 * dt)`：

```javascript
if (dist < 0.6) {
    player.takeDamage((this.damage || 20) * dt)
}
```

---

## 3. textures.js

### 3.1 新增三个纹理生成方法

`generateFastEnemy()`（橙色三角脸）、`generateTankEnemy()`（紫色方脸）、`generateGhostEnemy()`（青色椭圆脸）。结构与现有 `generateEnemy()` 相同，换颜色和形状。

### 3.2 generateAll() 注册

```javascript
this.textures[203] = this.generateFastEnemy()
this.textures[204] = this.generateTankEnemy()
this.textures[205] = this.generateGhostEnemy()
```

---

## 4. game/GameScene.js

### 4.1 敌人类型配置表

```javascript
const ENEMY_TYPES = [
    { tex: 201, speed: 1.2, hp: 100, damage: 20 },
    { tex: 203, speed: 2.5, hp: 60,  damage: 10 },
    { tex: 204, speed: 0.7, hp: 200, damage: 35 },
    { tex: 205, speed: 1.0, hp: 80,  damage: 25 },
]
```

### 4.2 constructor 新字段

```javascript
this.score = 0
this.wave = 0
```

### 4.3 _spawnEnemy(cx, cy)

```javascript
_spawnEnemy(cx, cy) {
    let t = ENEMY_TYPES[Math.floor(Math.random() * ENEMY_TYPES.length)]
    this.spriteManager.add(new Sprite(cx + 0.5, cy + 0.5, t.tex, {
        type: 'enemy', speed: t.speed, hp: t.hp, damage: t.damage
    }))
}
```

### 4.4 _placeSprites / _respawnEnemies 改用 _spawnEnemy

`_placeSprites`：初始 4 个，用 `_spawnEnemy` 替代手动 new Sprite。

`_respawnEnemies`：
```javascript
_respawnEnemies() {
    this.wave++
    if (this.wave >= 5) {
        this.state = 'win'
        this.overlay.show()
        this.overlay.drawWin()
        return
    }
    let empties = this._findEmptyCells()
    let px = this.player.position.x, py = this.player.position.y
    empties = empties.filter(([cx, cy]) => {
        let dx = cx + 0.5 - px, dy = cy + 0.5 - py
        return Math.sqrt(dx * dx + dy * dy) > 2
    })
    for (let i = empties.length - 1; i > 0; i--) {
        let j = Math.floor(Math.random() * (i + 1))
        ;[empties[i], empties[j]] = [empties[j], empties[i]]
    }
    let count = Math.min(4 + this.wave, empties.length)
    for (let i = 0; i < count; i++) {
        let [cx, cy] = empties[i]
        this._spawnEnemy(cx, cy)
    }
}
```

### 4.5 addScore

```javascript
addScore(pts) { this.score += pts }
```

### 4.6 reset() 重置

```javascript
this.score = 0
this.wave = 0
```

---

## 5. game/Game.js

### 5.1 敌人死亡加分

在 `update()` 中 `dead` 分支追加：

```javascript
if (dead) {
    if (window.audioManager) window.audioManager.playEnemyDeath()
    this.scene.addScore(100)
}
```

### 5.2 HUD 显示分数

在 FPS 区域下方：

```javascript
ctx.fillStyle = 'rgb(255,220,80)'
ctx.font = 'bold 14px monospace'
ctx.fillText('SCORE: ' + this.scene.score, width - 14, 64)
```

---

## 6. Background.js

### 6.1 worldMap 门格改墙

`101` → `4`，`102` → `4`。

### 6.2 drawWall 填满格子

```javascript
drawRect(ctx, color, x * unit, y * unit, unit, unit)
```
去掉 `+2 -4` 边距。

### 6.3 删除门相关代码

删除：`this.doors`、`this.doorColor`、`_initDoors()`、`isDoor()`、`isDoorPassable()`、`toggleDoor()`、`updateDoors()` 方法。删除 `drawWall` 中门绘制分支。

---

## 7. 任务清单

| 编号 | 任务 | 状态 | 文件 |
|------|------|------|------|
| 1 | tryMove 7点+对角 + cellIsEmpty 去门 | ✅ | `Player.js` |
| 2 | 删 E 键 + tryInteractDoor | ✅ | `Player.js` |
| 3 | 删 FOV 边界线 + drawRays 步长 5° | ✅ | `Player.js` |
| 4 | 敌人移动 5点+对角+去门 + damage 字段 | ✅ | `game/Sprite.js` |
| 5 | 3 种新敌人纹理 | ✅ | `textures.js` |
| 6 | 敌人随机类型 + 计分/波次/5波通关 | ✅ | `game/GameScene.js` |
| 7 | 杀敌加分 + HUD 分数 | ✅ | `game/Game.js` |
| 8 | 门改墙 + 填满格子 + 删门代码 | ✅ | `Background.js` |
| 9 | 删门时误删构造函数 `}`，补充闭合大括号 | ✅ | `Background.js` |
| 10 | drawDirArrow 删黄线，避免与白色中心射线重叠 | ✅ | `Player.js` |
| 11 | `_newCanvas` 首次 getContext 设 willReadFrequently | ✅ | `textures.js` |
| 12 | `_placeSprites` Fisher-Yates 洗牌防并排 | ✅ | `game/GameScene.js` |
| 13 | Sprite 构造函数补 `this.damage` 字段 | ✅ | `game/Sprite.js` |
| 14 | drawRays 中心射线改白色 `(255,255,255,0.8)` | ✅ | `Player.js` |
