# 子弹穿墙 + 射程修复 — 实现方案

> 2026-07-06

## 改动概述

3 个文件，改动量小，目标精准。

---

## 改动 1：`game/Weapon.js` — 新增视线检测

### 新增方法 `_hasLineOfSight(sprite)`

```javascript
// 检测玩家到精灵之间是否有墙体阻挡（离散采样）
_hasLineOfSight(sprite) {
    const player = this.player
    let dx = sprite.x - player.position.x
    let dy = sprite.y - player.position.y
    let dist = Math.sqrt(dx * dx + dy * dy)
    let ndx = dx / dist
    let ndy = dy / dist

    let steps = Math.max(2, Math.floor(dist * 4))
    for (let i = 1; i <= steps; i++) {
        let t = i / steps
        let cx = Math.floor(player.position.x + ndx * (dist * t))
        let cy = Math.floor(player.position.y + ndy * (dist * t))
        if (cx < 0 || cy < 0 || cx >= this.bg.columns || cy >= this.bg.lines) return false
        if (this.bg.worldMap[cy][cx] !== 0) return false
    }
    return true
}
```

### 修改 `fire()` — 在命中判定中加入视线检测

在现有 `if (Math.abs(spriteScreenX - centerX) < hitRange && transformY < bestDist)` 条件中，增加 `&& this._hasLineOfSight(s)`：

```javascript
// 修改前：
if (Math.abs(spriteScreenX - centerX) < hitRange && transformY < bestDist) {
    bestDist = transformY
    bestHit = s
}

// 修改后：
if (Math.abs(spriteScreenX - centerX) < hitRange && transformY < bestDist && this._hasLineOfSight(s)) {
    bestDist = transformY
    bestHit = s
}
```

---

## 改动 2：`game/Sprite.js` — 子弹记录发射点

### 修改 `update()` — 远程敌人发射子弹时附加 originX/originY

```javascript
// 修改前（第 100 行附近）：
return { projectile: { x: this.x, y: this.y, ndx, ndy, speed: 4, damage: 15 } }

// 修改后：
return { projectile: { x: this.x, y: this.y, ndx, ndy, speed: 4, damage: 15,
                        originX: this.x, originY: this.y } }
```

---

## 改动 3：`game/GameScene.js` — 敌人子弹射程修正

### 修改 `update()` — 射程检测改为从发射点算 10 格

```javascript
// 修改前（第 216-217 行）：
let pdx = p.x - this.player.position.x, pdy = p.y - this.player.position.y
if (Math.sqrt(pdx * pdx + pdy * pdy) > 15) {

// 修改后：
let odx = p.x - (p.originX !== undefined ? p.originX : p.x)
let ody = p.y - (p.originY !== undefined ? p.originY : p.y)
if (Math.sqrt(odx * odx + ody * ody) > 10) {
```

用 `!== undefined` 做兼容，防止已有飞行中的子弹没有 originX 字段时报错。

---

## 验证方式

1. 进入游戏，找一堵墙后面的敌人，对准敌人射击——子弹应被墙挡住，敌人不受伤害
2. 让远程敌人（绿色）朝你发射子弹，观察子弹飞过约 10 格后是否自动消失
3. 正常射击无遮挡的敌人，确认命中功能不受影响

---

## 改动清单

| 文件 | 改动类型 | 改动量 |
|------|----------|--------|
| `game/Weapon.js` | 新增 `_hasLineOfSight` 方法 + `fire()` 加 1 个条件 | +18 行 |
| `game/Sprite.js` | 子弹对象加 2 个字段 | 改 1 行 |
| `game/GameScene.js` | 射程检测逻辑修正 | 改 3 行 |
